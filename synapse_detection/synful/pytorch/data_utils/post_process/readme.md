## Saving synapse predictions to sqlite
The `synapse_to_sqlite.py` enables you to convert the synapse predictions under the synapse_dir into a sqlite database.
This is particularly helpful for saving millions of annotations in a structured format.

## Commands
Follow the commands in the `db_commands.md` file.
